# 이 파일을 .env로 복사하고 값을 채우면 실 OpenAI API 연동으로 전환됩니다.
# 비워두면(OPENAI_API_KEY 없음) 자동으로 mock 모드로 동작합니다 — 키 없이도 전체 파이프라인 시연 가능.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# true로 두면 실 키가 있어도 강제로 mock만 사용합니다 (데모/리허설용 탈출구).
AEO_FORCE_MOCK=false

PORT=4174
WEBHOOK_TOKEN=
